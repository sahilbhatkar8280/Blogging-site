{
    "scripts": {
      "postinstall": "npm install --prefix backend && npm install --prefix frontend",
      "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
      "dev:server": "npm run dev --prefix backend",
      "dev:client": "npm start --prefix frontend",
      "build": "cd frontend && npm install && cross-env REACT_APP_API_URL=/api npm run build",
      "prestart": "npm run build",
      "start": "npm run start:prod",
      "start:prod": "cross-env NODE_ENV=production npm start --prefix backend"
    },
    "dependencies": {
      "cross-env": "^7.0.3"
    },
    "devDependencies": {
      "concurrently": "^9.0.1"
    }
  }